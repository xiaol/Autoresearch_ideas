# Voiceover Draft

Many teams think they have a modeling problem when they actually have a data problem. The feature changed meaning, the join duplicated rows, the timestamp logic leaked the future, or the serving path no longer matches training.

That is why data engineering is not background plumbing for ML. It is part of the model's truth.

The data readiness harness exists to force explicit review before expensive modeling work begins.
